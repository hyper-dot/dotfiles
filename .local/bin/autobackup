#!/bin/bash

timestamp=$(date +"%Y-%m-%d %H:%M:%S")

cd $HOME/my-notes/ && git add . && git commit -m "AUTO COMMIT $timestamp" && git push

cd $HOME/dotfiles && git add . && git commit -m "AUTO COMMIT $timestamp" && git push
